namespace E.Standard.Extensions.Enum;

static public class EnumExtensions
{
    //static public T AddFlag<T>(this T flags, T flag, bool add)
    //    where T : System.Enum
    //{
    //    int val = Convert.ToInt32(flags) | Convert.ToInt32(flag);

    //    return add ? (T)Convert.ChangeType(val, typeof(T)) : flags;
    //}
}
