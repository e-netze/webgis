namespace E.Standard.Api.App.Extensions;

static public class FeatureExtension
{
    //static private CryptoImpl _crypto = new CryptoImpl(new CryptoServiceOptions());

    //static public string CalcHashCode(this Feature feature)
    //{
    //    var sortedAttributes = feature.Attributes.OrderBy(f => f.Name).ToArray();

    //    StringBuilder sb = new StringBuilder();

    //    for(int i=0,to=sortedAttributes.Length;i<to;i++)
    //    {
    //        var attr = sortedAttributes[i];

    //        sb.Append(attr.Value);
    //    }

    //    return _crypto.Hash64_SHA1(sb.ToString());
    //}
}
